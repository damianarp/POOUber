// Creamos la clase Account, le pasamos los parámetros obligatorios y automáticamente ya los toma como parámetros del método constructor y los instancia.
function Account(name, document) {
    this.id;
    this.name = name;
    this.document = document;
    this.email;
    this.password;
}